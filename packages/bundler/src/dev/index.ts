import { App } from "@tracer/node";
import { Bundler } from "../../types/bundler";
import { BundlerConfigs } from "../../types/options";

export async function dev(options: BundlerConfigs, app: App): ReturnType<Bundler["dev"]> {
    return
}